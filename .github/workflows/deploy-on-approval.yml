# ============================================================
# DISABLED - Replaced by approve-command.yml
#
# All deployment logic is now handled by the /approve command workflow.
# This file is kept for reference only.
# ============================================================
name: Deploy on PR Approval (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "This workflow is disabled. Use /approve command on a PR instead."
